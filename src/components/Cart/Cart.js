import React from 'react';
import './Cart.css'

const Cart = ({cart}) => {
    console.log(cart);
    return (
        <div className='cart'>
            <h1>Selected Items </h1>
            
        </div>
    );
};

export default Cart;